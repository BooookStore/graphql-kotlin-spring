type Query {
    bookById(id: ID): Book
    hero(episode: Episode): Character
}

type Book {
    id: ID
    name: String
    pageCount: Int
    author: Author
}

type Author {
    id: ID
    firstName: String
    lastName: String
    age: Int
}

type Character {
    name: String!
    appearsIn: [Episode!]!
}

enum Episode {
    NEW_HOPE
    EMPIRE
    JEDI
}
